<!DOCTYPE html>
<html>
<head>
  <style>
    .container { font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; }
    .logo { text-align: center; }
    .content { padding: 20px; background: #f9f9f9; border-radius: 8px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="{{logoUrl}}" alt="Logo do Sistema" style="max-width: 150px;">
    </div>
    <div class="content">
      <h2>{{subject}}</h2>
      <p>{{message}}</p>
      <p>Atenciosamente,<br>Equipe {{companyName}}</p>
    </div>
  </div>
</body>
</html>